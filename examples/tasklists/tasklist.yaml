- name : 1. System update
  tasks:
    - !apt
      upgrade: true # Upgrade the whole system

- name : 2. Prerequisites installation
  tasks:
    - !apt
      state: present # present | absent
      package: build-essential

    - !apt
      state: present
      package: git

- name : 3. Core business tool installation
  tasks:
    - !apt
      state: present
      package: the-engine